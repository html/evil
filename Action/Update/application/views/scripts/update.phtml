<?php
if(isset($this->form))
{
    echo $this->form;

    foreach($this->evilAutoloads as $name => $function)
    {
        if(isset($this->$name))
            echo call_user_func($function, $this->$name);
    }
}
else
    echo 'Missed form';

?>
<?=__FILE__?>